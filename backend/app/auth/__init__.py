"""Authentication package — Phase 2.

Provides:
  password  — Argon2 hashing and verification
  jwt       — JWT access token encoding/decoding
  dependencies — FastAPI DI helpers for the current authenticated user
"""
